package com.devgroup.medicapp;

import androidx.appcompat.app.AppCompatActivity;
import android.os.Bundle;

public class PastilleroActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);

        setContentView(R.layout.activity_pastillero);

        // A partir de aquí, puedes empezar a agregar la lógica de tus botones,
        // inicializar TextViews, RecyclerViews, etc.
        // Por ejemplo:
        // Button botonAgregar = findViewById(R.id.btn_agregar_pastilla);
        // ...
    }
}